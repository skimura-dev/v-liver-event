backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "images/uploads"

# ★ここをあなたのGitHub情報に書き換えてください★
# 例: user123/v-liver-event
repo: skimura-dev/v-liver-event

collections:
  - name: "red_team"
    label: "🔴 赤チーム"
    folder: "data/red"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "名前", name: "name", widget: "string"}
      - {label: "リンクURL", name: "link", widget: "string", required: false}
      - {label: "画像", name: "image", widget: "image"}
      
  - name: "blue_team"
    label: "🔵 青チーム"
    folder: "data/blue"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "名前", name: "name", widget: "string"}
      - {label: "リンクURL", name: "link", widget: "string", required: false}
      - {label: "画像", name: "image", widget: "image"}

  - name: "yellow_team"
    label: "🟡 黄チーム"
    folder: "data/yellow"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "名前", name: "name", widget: "string"}
      - {label: "リンクURL", name: "link", widget: "string", required: false}
      - {label: "画像", name: "image", widget: "image"}